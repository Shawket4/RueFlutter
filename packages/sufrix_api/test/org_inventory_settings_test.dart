import 'package:test/test.dart';
import 'package:sufrix_api/sufrix_api.dart';

// tests for OrgInventorySettings
void main() {
  final OrgInventorySettings? instance = /* OrgInventorySettings(...) */ null;
  // TODO add properties to the entity

  group(OrgInventorySettings, () {
    // Stock-count variance tolerance (percent). A counted row whose |difference| is at least this percent of expected is flagged and needs a reason.
    // double stocktakeVarianceThresholdPct
    test('to test the property `stocktakeVarianceThresholdPct`', () async {
      // TODO
    });

  });
}
